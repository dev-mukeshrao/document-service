import { DocumentInterceptor } from './document.interceptor';

describe('DocumentInterceptor', () => {
  it('should be defined', () => {
    expect(new DocumentInterceptor()).toBeDefined();
  });
});
